{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
    {% for type, message in app.flashes %}
        <div class="alert alert-{{ type }}" role="alert">
            {{ message | join(', ') }}
        </div>
    {% endfor %}
{% endblock %}
